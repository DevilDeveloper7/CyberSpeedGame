package ru.devildeveloper74.enums;

public enum BonusImpact {
    MULTIPLY_REWARD,    // Multiply the final reward (e.g., 10x, 5x)
    EXTRA_BONUS,        // Add extra value to the reward (e.g., +1000, +500)
    MISS                // No impact (e.g., "MISS" symbol)
}
